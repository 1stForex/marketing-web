export interface OfferProps {
  badgeTitle?: string;
  title: string;
  specialTitle: string;
  description: string;
  descriptionWidth?: number;
  subDescription?: string;
  buttonText?: string;
  onClick?: () => void;
}
